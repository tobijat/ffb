<IfModule mod_rewrite.c>
    RewriteEngine On

    # DocumentRoot stays the repo root; Laravel lives in /public.
    RewriteRule ^$ /public/index.php [L]

    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>
